#include<stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    for(int row=1;row<=n;i++){
        for(int col=1;col<=n-1;col++){
            printf("%d",col);
        }
        printf("%d",col);
    }
    return 0;
}